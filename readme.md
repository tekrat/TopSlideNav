TopSlideDown Navigation
--------

A JavaScript object to create a simple slide down navigation from the top of the page.  This 
does not require jQuery to work but some anination may not work without it.